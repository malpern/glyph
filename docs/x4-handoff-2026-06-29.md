# Glyph (phone) → X4 firmware handoff — 2026-06-29

Status update + coordination request for the **X4 firmware agent**. Canonical
addressing/protocol remains `x4-auto-reader/docs/addressing-contract.md`; flag if
anything below has drifted from it.

## 1. Highlight-granularity setting is built (default = Sentence)

The phone emits to the X4 based on a user setting (`HighlightGranularity` →
`RemoteSessionController`):

| Mode | Phone sends, and when |
| --- | --- |
| **Sentence** | `{"cmd":"highlight","spine":S,"para":P,"sent":N}` on each spoken sentence |
| **Paragraph** | `{"cmd":"highlight","spine":S,"para":P}` (**`sent` omitted**) on each new paragraph |
| **Page** | `{"cmd":"goto","spine":S,"para":P}` on each new paragraph |
| **Off** | nothing sent |

**Confirm:** the paragraph-mark path (`highlight` with no `sent`) is still LIVE and
acks `{"evt":"hl",...,"sent":-1,...}`, and one command per sentence/paragraph won't
starve button polling at natural TTS cadence.

## 2. Phone voice is now selectable (Apple / OpenAI / ElevenLabs)

Resolves the earlier "both voices sound like OpenAI" note — the phone's TTS voice is
now independent and user-configurable (with prefetch + Apple fallback). No protocol
impact; FYI only.

## 3. Ready for the real-device end-to-end test

The phone fully implements page-follow (`goto`), sentence highlight, paragraph mark,
the position bridge (adopts `pos`/`ready` into cloud sync), and auto-reconnect. All
verified in code/mock but never against a real X4. **Let's schedule a live run** to
validate the three granularities + the position bridge end-to-end — the one open item
gating sign-off. (Phone task X4.)

## 4. Reverse-resume — need a decision

Goal: opening the X4 jumps to the phone's *newer* cloud position. The phone is the
brain (cloud source of truth), so proposed policy:

> **On `ready`, the phone sends a `goto` to its current cloud position (phone wins on
> connect); the X4's `pos` events override during the session (user took control).**

Does the X4 accept a `goto` right after `ready` without fighting its own restore? If
you'd rather the phone defer to the device, tell me what freshness/timestamp marker
you can put in `ready` so the phone can compare and decide.

## 5. Confirm `pos` behavior

On a physical page turn you emit `{"evt":"pos","spine":S,"para":P}` and ignore inbound
nav ~1s — correct? The phone treats `pos` as "user took control": pause TTS and follow.

## 6. Not yet wired (say when you want them)

`open{bookId}` and `state{playing,rate}` exist in the phone's codec but aren't sent
yet. Ping when the firmware can use them.
