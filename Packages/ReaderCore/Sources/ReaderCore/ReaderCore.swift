// ReaderCore
//
// Public surface of the Core package. The real types live in Models/,
// ReadingState/, Persistence/, and Services/. This file documents the
// module and exposes a version marker used by the scaffold build check.

public enum ReaderCore {
    /// Marker used by the M1 scaffold to prove the package links and runs.
    public static let version = "0.1.0"
}
